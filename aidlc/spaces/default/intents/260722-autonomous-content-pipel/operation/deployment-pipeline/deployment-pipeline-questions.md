# Deployment Pipeline — Decisões

[Answer]: Deploy via `cloudbuild-pipeline.yaml`. Primeiro deploy requer setup manual dos Cloud Run Jobs via `agents/pipeline/infra/setup_jobs.sh`. Deploys subsequentes automáticos via Cloud Build trigger.
