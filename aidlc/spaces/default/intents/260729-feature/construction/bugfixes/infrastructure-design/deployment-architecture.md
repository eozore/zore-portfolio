# Deployment Architecture
Sem mudança na arquitetura de infra GCP. Todos os serviços já em Cloud Run / Cloud Run Jobs.
BUG2: deploy coordenado de cmo-agent + heygen-callback + video-editor-job via cloudbuild.yaml.
